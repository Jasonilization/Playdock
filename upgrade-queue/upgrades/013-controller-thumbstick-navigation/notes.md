Risk: medium. Compiles clean, architecturally sound (reuses the exact GCControllerDirectionPad
type Apple already gives both thumbsticks - same type the D-pad uses), but not yet confirmed live
with real controller hardware. Before shipping: connect a real controller, confirm both sticks
move focus in-app, then drop to low risk.

# GameModeView: localize the sort menu + search bar

Runs the sort-option labels and the search field placeholder / Sort picker through L(_:).

Applies on top of: 120-gamemodeview-localize-header-chrome

# GameModeView: wire cornerFloaters + settingsOverlay into body

Replaces the old inline steamFloatingIcon call site with cornerFloaters, adds settingsOverlay to the root ZStack, and excludes the settings overlay from the dashboard controller layer.

Applies on top of: 108-gamemodeview-settings-launcher-scaffold

# Add the L(_:) localization engine

Playdock's hand-rolled localization layer: AppLanguage (system + 7 locales, endonyms), the free L(_:)/LF(_:_:) lookup functions, and Localization (selected/active resolution, English-key fallback, a two-form plural helper). Ships with an empty table set - each language is added on top, one at a time.

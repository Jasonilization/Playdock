# GameModeView: point the gear button and controller at openSettings()

The header gear button and the controller 'toolbar' action now open the full-window panel instead of setting the old sheet flag.

Applies on top of: 117-gamemodeview-wire-launcher-into-body

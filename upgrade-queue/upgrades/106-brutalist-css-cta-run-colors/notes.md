# skins.css: Brutalist .cta/.run fill → var tokens

`.brut .cta,.brut .run` fixed #111/#fff → var(--fg)/var(--bg), matching the native playdockButtonLook inversion in 089. (The green `.brut .run{background:#06d6a0}` override just below is untouched.)

Pairs with 089 (native button look).

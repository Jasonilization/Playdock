# Brutalist button: invert to a theme-aware solid fill

The fixed #111 fill was near-invisible on Brutalist's own dark ground - the button read as a hollow outline. Inverted to var(--fg)/var(--bg) the way Luxury's real .cta already is, keeping every other Brutalist button trait.

Pairs with 088 (hard shadow) and 094 (skins.css .cta/.run) - same visual fix in the three places it lives.
